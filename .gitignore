node_modules/
.pnpm-store/
*.tsbuildinfo
.DS_Store
.evocode/

# `lib/` build outputs are COMMITTED so `dsh plugin add github:jhonden/my-dsh-plugins/...`
# works without a local build (pnpm installs straight from the git tree).
# Rebuild (`pnpm build`) and commit whenever src/ changes.
# personal recordings dropped into the plugin dir (never commit)
plugins/dsh-session-notify/*.m4a
